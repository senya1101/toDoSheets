<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешение на отправку уведомлений (нужно для Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Разрешение на управление режимом "Не беспокоить" (для фокус-таймера) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoListAPP"
        android:icon="@mipmap/ic_launcher"
        tools:targetApi="31">

        <!-- Главный экран приложения -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Экран добавления/редактирования задачи -->
        <activity
            android:name=".activities.AddTaskActivity"
            android:exported="false" />

        <!-- Экран со списком завершённых задач -->
        <activity
            android:name=".activities.CompletedTasksActivity"
            android:exported="false" />

        <!-- Экран фокус-таймера (Pomodoro) -->
        <activity
            android:name=".activities.TimeTrackerActivity"
            android:exported="false"
            tools:ignore="Instantiatable" />

        <!-- Экран отладки (тестирование БД, экспорт/импорт) -->
        <activity
            android:name=".activities.DebugActivity"
            android:exported="false" />

        <!-- Виджет для домашнего экрана (показывает список задач) -->
        <receiver
            android:name=".widget.TaskWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/task_widget_info" />
        </receiver>

        <!-- Сервис для виджета (загружает данные задач) -->
        <service
            android:name=".widget.TaskWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

    </application>

</manifest>
